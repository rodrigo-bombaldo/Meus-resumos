Se importar pacote 
para saber o tipo da variável
usa o releat.Typeof(nome)
Aí ele descobre o o tipo da variável.
se não especificar ele cologca o folat64
se quiser encurtar ocódigo nem precisa usar variável:=nomeé muito utilizado para codar em Go.
O operador curto de variável tem muitas outras veantagens que serao abordadas no decorrer do curso.
Agora vamos fazer um menu em que o jogador consegue escolher o que ele deseja fazer.
1 Iniciar monitoramento dos sites
2 Exibir os logs
0  Sair  do programa
que capturar faz parte do fmt
fmt.Scanf
aceita o tipo da variável e o que encurtarvar comando int
espera %d = inteiro e salvana variável comando &
& = endreço da variável.
É tão amis fácil do qeu Java script
quando não declara variável é = 0 que é falso então dá um ero no programa.
O endereço da minha variável comando é  é um núemro em hexadecimal que define o endereço da memória do computador.

por que precisa especificar na scanf 
E criou uma função mais simples que é a funçaõ scan não precisa dizer qual é o tipo da variável
quando é falso vale zero

var nome = "Carlos"
Não precisa declarar string
Tem o nome := 'Julia'
Já infere qual tipo da variável.
scanf precisa de modificador %d para inteiro%s para string%f para número float decimal.
Com scan não precisa do modificador.
acessa com &variávelpacote reflect para descobrri o tipo das variáveis.
reflect.TypeOf(nome)
Se você  declarar variável e não for usada, dar erro no Go.

Estamos agora na aula 3 do curso de Go Lang
dá para pegar escolha do usuário para indicar o que ele escolheu.
Vamos usar o if no mundo do Go.
não precisa abrir e nem fechar parentêses.
if comando == 1
expressão que retorna algo booleano
tem ser verdadeiro ou falso.
não dá para colocar só o nome da variável como se fosse no Java scriptif coamndo==1

